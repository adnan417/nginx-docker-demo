version: "3.8"
services:
  app1:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - APP_NAME=App1
    # ports:
    #   - "3001:3000"

  #instead of defining multiple services, we can scale a single service 
  app2:
    build:
      context: .
      dockerfile: Dockerfile 
    environment:
      - APP_NAME=App2
    # ports:
    #   - "3002:3000"

  app3:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - APP_NAME=App3
    # ports:
    #   - "3003:3000"
    
  nginx:
    build:
      context: .
      dockerfile: nginx.Dockerfile
    container_name: nginx_proxy
    ports:
      - "8080:80"